from setuptools import setup, find_packages

setup(
    name="discobot_actions",
    version="0.1",
    author="Argentavis",
    description=
    "This package is used to interact with discord bots, either by clicking on emoji reactions, or by responding in text when the bot asks for data.",
    packages=find_packages(),
)